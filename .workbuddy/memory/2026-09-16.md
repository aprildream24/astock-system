# 2026-09-16 工作日志

## 抓取深度 260 → 40 根改造（用户指令）

用户原话：
> 「先尝试40根，可以适当增加，这个260根是什么意思？然后把推送全部推送一遍，
> 再检查看看有没有其他错误」

### 「260 根」是什么意思（答用户问）
`--days N` = **每只票往回拉 N 个交易日的日K**（每天 1 根）。
260 根 ≈ 一年历史（A 股年均 ~243 个交易日）。

### 为什么降到 40：全仓回看深度审计（硬数据）
脚本扫描 `pipeline/*.py` 全部切片表达式，真实结果：

| 回看 | 位置 | 用途 |
|---|---|---|
| **30** | `engines.py:262` `rows[-30:]` | **最大真实回看** |
| 20 | `engines.py:420` `rets[-20:]` | 波动率比 |
| 20 | `engines.py:140` `vols[-20:]` | 量比 |
| 20 | `engines.py:123` `dailies[-20:]` | 加速度 |
| ≤10 | 其余全部 | 展示/截断 |

即「**用 30~32 根、拉 260 根**」= 白耗约 8 倍解析/写库量。
40 根留 +33% 余量（40 vs 30）。

### 收益实测（腾讯端点，单票真拉）
| days | 响应体积 | 相对 260 |
|---|---|---|
| 20 | 2,833 B | 1/6.3 |
| **40** | **4,037 B** | **1/4.4** |
| 260 | 17,817 B | 1× |

全市场 4936 只外推：
- 260 → 83.9 MB/次；40 → 19.0 MB/次
- **少下载 65 MB、少解析 65 MB JSON / 次（省 77%）**
- 按 30 次/月：**月省 1.9 GB 流量**

### 改动（3 个文件 6 处）
`pipeline/fetch_daily.py`：
- 模块 docstring 补「抓取深度」章节（写明调参依据与历史不受影响）
- 新增常量 `DEFAULT_DAYS=40` / `INC_DAYS=20` / `MAX_FULL_DAYS=40`
- `def fetch_daily(days=DEFAULT_DAYS, ...)`（原 260）
- **`full_days = min(days, MAX_FULL_DAYS)`**（原 `min(days,40) if days<=20 else days`）
  —— 修掉一个隐藏隐患：默认 days=260 时全量路径真拉 260 根，冷库首拉必超时
- 增量路径 `min(days, INC_DAYS)`
- 指数补拉 `days=full_days`（原 `days=days`，同样越界）
- CLI `--days` default=DEFAULT_DAYS

`pipeline/core.py`：
- `def kline_batch(codes, days=40, ...)`（原 **260**）—— 底层默认也该收紧，
  防遗漏传参的调用方静默拉一年历史

`tests/test_zero_push_20260915.py`：
- 原断言钉死字面 `min(days, 40)` → 改**语义断言**：
  ① `MAX_FULL_DAYS` 常量存在且 == 40
  ② `full_days = min(days, MAX_FULL_DAYS)`
  ③ 可执行行不得再有 `... if ... else days` 旧写法（注释里提旧写法不算）
  ④ `DEFAULT_DAYS = 40`
  ⑤ 顺手补 `import re`（原文件缺，assertRegex 之前靠别的来源？实测需补）

### 历史数据不受影响（重要澄清）
库里 1996-01-26 至今的存量 **1,286,349 行** K线是**只增不改**的。
`--days` 只决定「每次新拉多少根」，**不删旧数据**。
实测每票平均 258 根（min 1 / max 262）—— 所以想算 250 日年线**依然有料**，
只是不再每天重复拉旧数据。要补更长历史请用 `tools/fetch_all.py --days N`。

### 回归
`tests/run_regression.py` → **PASS=313 FAIL=0 ALL_PASS ✓**（两次确认：
改完 fetch_daily 后一次、改完 core.py + 指数封顶后一次）
基线 `tests/baseline.json` = 313，未变（新增的是语义断言，条数不变）。

### 部署
- `gh_sync.py`（REST API，本地无 git）→ **103 文件，commit `7bbb901ebe`**
- 远端核验三文件均含新常量：
  - `pipeline/fetch_daily.py` `MAX_FULL_DAYS = 40` ✓ sha=87c578a7
  - `pipeline/core.py` `def kline_batch(codes, days=40` ✓ sha=8fcfe367
  - `tests/test_zero_push_20260915.py` `MAX_FULL_DAYS` ✓ sha=ec2c8d3a
- ⚠️ commit message 仍是旧文案（`feat: 推送内联样式重排...`）——
  `gh_sync.py:96` 的 message 是硬编码的，已顺手改成
  `chore: 抓取深度 260→40 根 + 全量路径封顶（MAX_FULL_DAYS）`（下次推送生效）

### 通道真验（用户要求「把推送全部推送一遍」）
- `config/notify.json`：primary=**pushplus**，token 有效，`push_dry_run=False`
- `notifier.push('selftest_depth40', title, html, force=True)` →
  **`{"sent": true, "status": "sent", "results": {"pushplus": {"status":"sent","detail":"ok"}}}`**
- 账本 `dist/push_ledger.json` 已入账（13 条），key=`ae7e56b0f211b21a`
- ⚠️ `notifier.push()` 签名是 **`push(mode, title, content, ...)`** ——
  mode 是**第一个位置参数**，写成关键字会 `TypeError: multiple values for 'mode'`。

### 待确认
- CI run `34994141036`（HEAD=7bbb901e，in_progress）—— 复验 40 根在 CI 真跑通。
